# 复制此文件为 .env 并填写真实值

# ─── MySQL ────────────────────────────────────────────────────
DB_HOST=localhost
DB_PORT=3306
DB_NAME=cloud_disk
DB_USER=root
DB_PASSWORD=your_strong_password

# ─── PostgreSQL（AI 服务用）───────────────────────────────────
PG_PASSWORD=your_pg_password

# ─── Redis ────────────────────────────────────────────────────
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=          # 留空表示不设密码

# ─── JWT ──────────────────────────────────────────────────────
JWT_SECRET=replace-with-a-very-long-random-secret-key
TOKEN_EXPIRES_HOURS=48

# ─── 初始账号 ─────────────────────────────────────────────────
ASUKA_ADMIN_USERNAME=admin
ASUKA_ADMIN_PASSWORD=replace-with-a-strong-admin-password
ASUKA_GUEST_USERNAME=guest
ASUKA_GUEST_PASSWORD=
ASUKA_GUEST_ENABLED=false

# ─── AI 服务 ──────────────────────────────────────────────────
AI_SERVICE_BASE_URL=http://localhost:8000
AI_SERVICE_KEY=your-internal-api-key
MASTER_TOKEN=your-master-download-token

# ─── 本地存储 ─────────────────────────────────────────────────
ASUKA_LOCAL_ROOT_WHITELIST=/tmp/asuka-file-list

# 注：S3 / 百度网盘等远程驱动的凭据（accessKey/secret、refreshToken 等）
# 在管理端按存储逐个配置，存于 storages.addition（数据库），不走全局环境变量。

# ─── DeepSeek ─────────────────────────────────────────────────
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxx

# ─── Graph RAG 知识库（AI 服务）───────────────────────────────
# LightRAG 的 PG 后端 DSN（asyncpg 原生格式，区别于 SQLAlchemy DSN）
POSTGRES_AGE_DSN=postgresql://postgres:your_pg_password@localhost:5432/asuka_ai
# 本地 embedding（bge-m3）
EMBED_PROVIDER=bge-m3
EMBED_MODEL=BAAI/bge-m3
# HuggingFace 镜像（直连不可达时用于下载 bge-m3；可改为官方 https://huggingface.co）
HF_ENDPOINT=https://hf-mirror.com
# LightRAG workspace 命名前缀：kb_{kbId}
LIGHTRAG_WORKSPACE_PREFIX=kb_
# Agent loop 迭代上限与整体超时（秒）
AGENT_MAX_ITERS=3
AGENT_TIMEOUT_S=60
